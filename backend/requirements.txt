fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
pydantic==2.7.1
requests==2.32.2
mistralai>=1.0.0
langchain==0.2.1
langchain-core==0.2.1
langchain-mistralai==0.1.7
tavily-python==0.3.3
httpx>=0.27.0
websockets>=12.0
# ARIA Backend — Test & Development Dependencies
# Install with: pip install -r requirements-test.txt

# ── Test framework ────────────────────────────────────────────────────────────
pytest==8.3.2
pytest-asyncio==0.23.8
pytest-cov==5.0.0
anyio[trio]==4.4.0          # needed by httpx async transport

# ── HTTP test client ──────────────────────────────────────────────────────────
httpx==0.27.0               # async ASGI test client

# ── Lint & type-check ─────────────────────────────────────────────────────────
ruff==0.5.5
mypy==1.11.0
types-requests==2.32.0.20240712

# ── Utilities ─────────────────────────────────────────────────────────────────
coverage[toml]==7.6.1       # used by pytest-cov under the hood