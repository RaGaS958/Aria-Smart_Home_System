# ─────────────────────────────────────────────────────────────────
#  ARIA — requirements.txt  (PRODUCTION only)
#  ⚠️  Test/lint deps live in requirements-test.txt — never install
#      those in the Docker image.
#
#  Conflict fix summary:
#    OLD: pydantic==2.7.1  → mistralai>=1.1.0 needs pydantic>=2.9.0
#    OLD: httpx==0.27.0    → mistralai>=1.6.0 needs httpx>=0.28.1
#    FIX: pin mistralai to 1.5.x (no httpx>=0.28 req), bump pydantic
# ─────────────────────────────────────────────────────────────────

# ── Web framework ─────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0

# ── Config & HTTP ─────────────────────────────
python-dotenv==1.0.1
requests==2.32.2
httpx>=0.27.0,<0.28.0

# ── Data validation ───────────────────────────
# pydantic 2.7.1 → too old; mistralai>=1.1.0 requires >=2.9.0
pydantic>=2.9.0,<3.0.0

# ── AI — Mistral SDK ──────────────────────────
# mistralai 1.6.0+ requires httpx>=0.28.1 which conflicts with
# langchain-mistralai 0.1.7 (httpx<1,>=0.25.2).
# 1.5.x is the last range compatible with httpx 0.27.x.
# Model names are just strings sent to the API — any 1.5.x SDK
# works fine with mistral-small-2506.
mistralai>=1.5.0,<1.6.0

# ── LangChain (tool decorators only — NOT the agent executor) ────
langchain==0.2.1
langchain-core==0.2.1
langchain-mistralai==0.1.7

# ── External APIs ─────────────────────────────
tavily-python==0.3.3

# ── WebSocket ─────────────────────────────────
websockets>=12.0